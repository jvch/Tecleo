#dibujo de un punto de un color aleatorio
import random
import turtle
t = turtle.Turtle()
r = random.randint(0, 255)
g = random.randint(0, 255)
b = random.randint(0, 255)
t.color(r, g, b)
t.dot(25)