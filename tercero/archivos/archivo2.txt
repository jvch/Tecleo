#Resultado de tirar un par de dados 100 veces y su suma
import random
for n in range(100):
    dice1 = random.randint(1,6)
    dice2 = random.randint(1,6)
    total = dice1 + dice2
    print(dice1, dice2, total)