#Crea 20 puntos en coordenadas aleatorias
import turtle
import random
t = turtle.Turtle()
for lines in range(20):
    x = random.randint(-100, 100)
    y = random.randint(-100, 100)
    t.goto(x, y)
    t.dot(25)